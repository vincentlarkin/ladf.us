{
  "name": "ladf.us",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:data": "node ./scripts/sync-district-data.mjs",
    "dev": "vite",
    "build": "vite build",
    "build:refresh": "npm run sync:data && npm run build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.0"
  },
  "dependencies": {
    "@turf/boolean-point-in-polygon": "^7.3.4",
    "@turf/simplify": "^7.3.4",
    "leaflet": "^1.9.4",
    "shpjs": "^6.2.0"
  }
}
